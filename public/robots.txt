# Everything on this site is public. Nothing here is disallowed.
User-agent: *
Allow: /

# ── AI crawlers, allowed on purpose ───────────────────────────────────────
# These are the agents behind AI answers and assistant citations. They are
# covered by the wildcard above; they are named explicitly because several of
# them treat an unnamed default as "no opinion" and because a future edit that
# tightens the wildcard should have to think about them separately.
#
# Most of them do NOT execute JavaScript, so what they can actually read is
# the static fallback inside #app in index.html and /llms.txt, not the 3D app.

User-agent: GPTBot
Allow: /

User-agent: OAI-SearchBot
Allow: /

User-agent: ChatGPT-User
Allow: /

User-agent: ClaudeBot
Allow: /

User-agent: Claude-User
Allow: /

User-agent: PerplexityBot
Allow: /

User-agent: Google-Extended
Allow: /

User-agent: Applebot-Extended
Allow: /

User-agent: CCBot
Allow: /

# A machine-readable summary of the public portfolio, kept in step with what
# the site shows: /llms.txt

# The canonical origin. It is also index.html's canonical and og:url, `site`
# in src/content/profile.ts, and the <loc> values in sitemap.xml; change all
# of them in one edit.
Sitemap: https://bhavyethakkar.netlify.app/sitemap.xml
